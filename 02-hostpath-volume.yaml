apiVersion: v1
kind: Pod
metadata:
  name: hostpath-demo
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      volumeMounts:
        - name: webdata
          mountPath: /usr/share/nginx/html
  volumes:
    - name: webdata
      hostPath:
        path: /tmp/webdata
        type: Directory